# M11 · 生图重构：改用羽刃自定义模式（生图效果对齐羽刃）

## 背景/目标
测试反馈：本项目生图效果比羽刃差很多。目标是"选品类+主件+导入白底图 → 直接出图上新"，最大化自动化。决定：生图这部分**只用羽刃(ele-business)的自定义模式**，本项目现有生图逻辑重构对齐它。

## 差距根因（已核对代码）
**底层 GptImageAgent 是同一套**（已迁入 gofu-cloud），差距在**编排/提示词策略**：
- **本项目 FlowController.buildMainPrompt**：静态字符串模板，`"将白底图中的产品融入场景，突出卖点X"`——**从不看实际产品图**，prompt 千篇一律 → 效果差。
- **羽刃自定义模式（两步）**：
  1. `/api/custom_analyze` → `analyzeCustomImagePrompts(userPrompt, whiteImgs, count)`：**Gemini 真看白底产品图**+用户要求，扩写出**针对该产品的多段可编辑提示词**。
  2. `/api/custom_generate`：**第1张串行建基调**(ref=白底图) → **第2~N张并行**(ref=白底图+第1张)，`buildSeriesPrompt` 加系列一致性约束。auto 比例按参考图推断(resolveAutoAspect)。

关键差异：羽刃**先用视觉模型分析真实产品图再生图**，本项目直接套模板。这是效果差的主因。

## 重构方案（分块）
### A. 迁入羽刃自定义模式生图核心（云端）
- `ImageGenerationService.analyzeCustomImagePrompts` 从 ele 迁入 gofu-cloud（已有 GptImageAgent/generateImageMulti/resolveAutoAspect/buildSeriesPrompt 基础）。
- FlowController.genAllMains 重构：先 analyzeCustomImagePrompts(白底图, 品类/卖点) 得产品专属 prompt → 第1张串行基调 → 2~N 并行(ref+第1张)。替换现有 buildMainPrompt 静态模板。
- 详情图/SKU 图沿用（详情已配对，SKU 走乐羽防比价）。

### B. 重绘 + 重新生成（对齐羽刃）
- 核查本项目现状：
  - 重生：GenController `/api/gen/regenerate`(已有?) + 前端 regenImage
  - 重绘：GenController `/api/gen/inpaint`(已有,mask) + 前端 repaintImage
- 对齐羽刃 `/api/inpaint`(局部重绘,mask) + 自定义模式单张重生逻辑。需对比现状补齐。

### C. 前端布局（一键生成区不浪费位置）
- 主图张数/主图比例/方案数/防比价模式 四个下拉紧凑排列(一行 flex-wrap 紧凑，不留大空位)。

## 待确认/待查
1. 本项目重绘/重生现状 vs 羽刃——需读 GenController + workbench regenImage/repaintImage 对比后定补齐范围。
2. 自定义模式的"用户要求(prompt)"从哪来？羽刃是用户手输；本项目要自动化，应由品类/卖点/主件名自动生成这段"要求"。
3. 是否保留乐羽 SKU 防比价贴图（SKU 图效果目前 OK？），还是 SKU 也改自定义模式？—— 用户说"生图只用羽刃自定义模式"，需确认是否含 SKU。

## 验证
- 编译；起服务：custom_analyze 出产品专属 prompt、genAllMains 走两步、重绘/重生可用。真实出图效果需密钥实跑对比羽刃。

---

## 现状核对增补（2026-07-15，据代码实查）

> 上文"差距根因/重构方案"部分已过时。以下为实查 gofu-cloud 代码 + 羽刃(ele-business-java)最新提交后的真实现状。

### 一、gofu-cloud 已完成的（M11 老版已迁入，非"从不看产品图"）
- `FlowController.genAllMains`(:158) 已跑两步：`analyzeCustomImagePrompts`(看白底图) → 首图串行建基调 → 2~N 并行 + `buildSeriesPrompt`。
- `buildMainPrompt`(:812) 静态模板**已降级为"分析失败兜底"**(:222/246)，不再是主路径。→ 原文"从不看产品图"作废。
- `resolveAutoAspect`(:1075 仿羽刃)、`generateImageMulti`、多 agent(GptImage/Gemini/Qwen/Hunyuan/Wan) 均已就位。
- 现有 `analyzeCustomImagePrompts`(ImageGenerationService:103) = **旧单次全量版**，与羽刃改 0c98172 之前同一起点。

### 二、与羽刃最新差距 = 单一提交 0c98172「按参考单元拆分」
合作者 07-15 两提交：`3aa43a8`(application.yml 3行环境加载，无关生图)、**`0c98172`(生图核心)**。
0c98172 把 `analyzeCustomImagePrompts` 由"单次全量"改两阶段：
1. **拆单元提取(并行)**：11 维度(构图/核心主体/人物/背景/文字/放大/对比/光影材质/色彩/风格/**禁止项**)，每任务只喂 1 参考图+1 维度，100-180字，强约束"不确定写'未见明确证据'不补造"。
2. **整合(并行)**：每目标图收齐证据 → 纯文本整合成固定字段方案。
价值：新增**禁止项单元 + 严禁改产品主体/结构/比例/接口/Logo/防穿模**，正对结构变形痛点(见 structlock-bidirectional-fidelity)。

### 三、合并 0c98172 的 delta（干净，但非纯拷贝）
1. 新增 `CustomPromptAnalysisPlan.java`(153行、零外部依赖) → 拷进 `com.gofu.cloud.service`。
2. `ImageGenerationService` 加 `requestGeminiCustomPromptAnalysisByUnit`/`requestQwenCustomPromptAnalysisByUnit` + helper。
3. 入口(:110)加开关。
**移植风险**：羽刃这些方法依赖 `executor`/`executeAnalysisWithRetry`/`promptTemplateLoader.load("prompt/custom-analysis-system.txt")`/`appendAnalysisImage`——gofu-cloud 不一定全有，需逐个对齐补齐，勿盲拷。

### 四、关键改动：开关按 size 分流（勿照抄羽刃 isEmpty）
羽刃开关 = `files.isEmpty() ? 旧 : 按单元`(0 vs ≥1)。但**单参考图会退化成"每张目标图只分到 1 维度"**（其余靠通用补全），对咱们"1 张白底主件"的主图流反而可能变差。
→ 应改为 **`files.size() <= 1 ? 旧全量 : 按单元`**：单图主件走旧全量，多图(风格迁移/导入成品图)才走单元拆分。

### 五、模型现状：生图模型 ≠ 分析模型（两笔账）
| 用途 | 现值 | 位置 | 换法 |
|---|---|---|---|
| 生图 | `gpt-image-2` | GptImageAgent **硬编码**(:192/302/338)+local.yml | 非改配置，须走 agent 路由切 GeminiImageAgent |
| 生图(prod) | `gemini-3.1-flash-image-preview`=**Nano Banana 2** | application.yml:32 | 已配好，待激活 |
| 分析(看图出prompt) | `gemini-3-pro-preview` | ImageGenerationService:33 硬编码 | 保持(大脑) |

- 换 Nano Banana 2 生图 = **provider/agent 路由问题**（GptImageAgent→GeminiImageAgent），非改字符串。待深挖 provider 选择逻辑。
- **成本交叉点**：件一"按单元拆分"翻倍的是**分析模型**调用(参考图×目标图)；件二换的是**生图模型**。两笔分开算。

### 六-补、provider 路由已查清（2026-07-15）
- 路由机制：`ImageGenerationService.agentMap` 按 `getId()` 装 5 agent（gpt-image/gemini/qwen-image/hunyuan/wan2.7）。`resolveAgent(agentId)` 传空/不命中→兜底 `DEFAULT_AGENT_ID="gpt-image"`。
- **全生图流唯一调用 FlowController:842 传 agentId=`null`** → 全走 gpt-image-2（genAllMains/重生/详情图都是）。
- **拦路石**：GeminiImageAgent 生图 URL(:135)=`imageBaseUrl + model + ":generateContent?key="+key` 是 **Google 原生格式**，但配的是**中转站 sk- key**（local.yml:31）。local.yml:30 注释警告"中转站 key 走原生接口会 401"。→ **直接切 agentId="gemini" 会 401**，GeminiImageAgent 是没跑通的半成品。
- 换 Nano Banana 2 = **agentId 路由(1行) + GeminiImageAgent 接口改造**，二选一：
  - A. 配 Google 原生 key(AQ.开头)走现有接口——有泄露风险(之前正因此弃用)。
  - **B.(推荐) 改 GeminiImageAgent 走中转站 OpenAI 兼容/图像端点(/v1 + Bearer)**——与分析链路(gemini-3-pro-preview 走中转站)同源、¥0.05 最便宜、无原生 key 风险。
- 待确认：中转站(linapi.net)对 `gemini-3.1-flash-image-preview` 的确切调用格式(端点/字段/返回)。

### 七、件一已完成（2026-07-15，编译通过）
合并羽刃 0c98172 单元拆分进 gofu-cloud：
- 新增 `com.gofu.cloud.service.CustomPromptAnalysisPlan`（153行，从羽刃拷入改包名）。
- `ImageGenerationService` 加 `requestGemini/QwenCustomPromptAnalysisByUnit` + `requestCustomPromptAnalysisByUnit`(两阶段编排) + `requestGemini/QwenCustomUnitAnalysis` + `appendAnalysisImage` + `normalizeIntegratedCustomSegment` + `CustomUnitAnalysisClient`/`TargetUnitResult`。
- 入口(:analyzeCustomImagePrompts)开关：**`files.size() > 1` 才走按单元**（单图主件保留旧全量，避免退化成每图仅1维度）。
- 基建全部复用现有(executor/executeAnalysisWithRetry/buildOpenAIChatRequest/custom-analysis-system.txt/getMimeType)，URL 构造对齐(gemini `/v1/chat/completions`、千问 `/chat/completions`、Bearer)。
- `mvn -pl gofu-server-cloud -am compile` = **BUILD SUCCESS**。
- **待实机验证**：需 ≥2 张参考图场景(风格迁移/导入多张成品图)真实跑一次，看单元拆分产物质量 vs 旧全量。

### 六、下一步（未做）
- [ ] 【件一后续】≥2 参考图实机验证单元拆分产物质量。
- [ ] 【件二】确认中转站 gemini-3.1-flash-image-preview 调用格式（Context7/中转站文档）。
- [ ] 【件二】做 B：改造 GeminiImageAgent 请求构造 → 走中转站；再改 agentId 路由换 Nano Banana 2。
- [ ] 合并 0c98172：先补基建(executor/retry/system prompt 文件)→ 拷 CustomPromptAnalysisPlan → 加 size<=1 开关。
- [ ] 编译验证 `mvn -pl gofu-server-cloud compile`。

