# M10 · 07.06 二轮反馈：根因分析（依据测试机真实日志 cloud.log/local.log）

> 关键：这次有测试机真实日志，推翻了之前的推断。下面每条都带日志证据。

## 日志还原的真相（v1.4 那次运行）
- 主图 6 张(aspect=1:1)、详情 6 张(aspect=16:9)、SKU 9 张(3色×3型号) **全部生成成功**并传 COS。
- 上新 `DONE:success`，9 个 SKU 图全上传拼多多(`SKU图检测：全部已上传 ✓`)。
- 中间那串 `花洒主图失败: Read/Connection timed out` 是**网络瞬断**(不是429限流！我上轮推断错了)，我加的退避重试**扛过去了**——超时后第 6/7/8 张 SKU 照样出图上传。

## 问题1：详情图尺寸 9:16（本轮更正）
日志证实详情图现在按 **aspect=16:9** 生成(上轮我按你拍板改的)。本轮更正为 **9:16 竖版**。改 FlowController.step2 详情图 aspect="9:16" + 尺寸。【直接做】

## 问题2：SKU 没全生成 + 上新用主图不用SKU图
**2a. 只生成选中方案**：日志只有"增压过滤款"一套的 9 张 SKU。runStep2 用 selectedPlanIndex 只跑一套——**属实**。
**2b. 上新用主图不用SKU图**：⚠️**日志显示上新用的是真实 SKU 图**(01.jpg 每个 SKU 名字不同、9 张各异)，不是主图。但代码 `ContextToListingMapper:112 s.setImgDir(cfg.getMainImgDir())` 明明写死主图目录——说明脚本另有按 SKU 名匹配图的逻辑救了场。**仍需修**：mapper 应显式传 `it.getImgDir()`(真实SKU图)，别依赖脚本兜底。这条按你反馈"上新用第一张主图"可能出现在**别的场景**(如某 SKU 生图失败时回退)，改成显式传真图更稳。
**2c. 缺「1套/全套」+「防比价模式」设置**：乐羽有 planMode(1套/4套) + templateId(防比价模板下拉/随机)。GOFU 后端 step2 已支持 templateId 入参、模板库(6模板)也在，**只差前端设置项**。

## 问题3：主图/详情/SKU 两侧白边 —— 找到确切元凶（日志实锤）
**元凶在 `GptImageAgent.prepareInputImage`(行225-258)**：
- 详情图 aspect=16:9，但**参考图是 1:1 的主图**(1024×1024)。
- prepareInputImage 把 1:1 参考图 letterbox 成 16:9 画布时——`g.setColor(Color.WHITE); g.fillRect(...)`(行239-240)**用纯白填充左右**，再把图居中画上去。
- 日志实证：`prepareInputImage: 1024x1024 -> letterbox 1536x1024`(反复出现)。
- 模型拿到"带白边的参考图"，图生图时**把白边一起复刻进输出** → 成品两侧白边。
- 另外 SKU 图先 `1254x1254 resized -> 1024x1024`(压方)，也是比例硬套的痕迹。

**你的指示**：暂时先用羽刃"自定义模式"逻辑，后续把全部模式加进来，具体和你讨论。

## 建议做法（★ 待你确认后动手）
1. **问题1**：详情图 aspect 改回 9:16。【明确，直接做】
2. **问题3 白边（最优先）**：letterbox 填充色从**纯白改成"边缘拉伸/模糊延展"或透明**，或**参考图不做letterbox**(直接传原图+prompt说明目标比例，让模型自己构图)。羽刃"自定义模式"实际就是**按目标 aspect 出图、不硬塞白边**。→ 我倾向：prepareInputImage 对非1:1时改用**边缘颜色延展填充**(取图边缘像素平均色/模糊拉伸)替代纯白，成本最低、白边立即消失。
3. **问题2b**：mapper 显式用 it.getImgDir()，缺失才回退主图。【明确，直接做】
4. **问题2a+2c**：前端加「设置」——方案数(1套/全套) + 防比价模式(模板下拉/随机)，对齐乐羽；runStep2 按 planMode 决定跑几套。

## 要和你讨论的 3 点
1. **白边修法二选一**：(A) letterbox 白色→边缘延展色(最小改动，白边消失，我推荐)；(B) 干脆不 letterbox 参考图、让模型按 aspect 自由构图(更彻底但可能改变构图风格)。你倾向哪个？
2. **"全部模式"范围**：你说后续把全部模式加进来——是指乐羽那 6 个防比价模板(sticker/45°喷水/拆解/人像/出水模式/3D瀑布)全部搬进前端可选？本轮先只做白边+9:16+设置项，全部模板作为下一个独立任务？
3. **多套方案出图量**：全套(4套)每套都出全部 SKU 图，图量×4、异步生图×4慢很多。确认要全出，还是"预览只出选中方案，上新时才补出对应方案"？
