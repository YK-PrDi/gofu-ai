# M10 · 07.08 反馈分析（先查后动，问题1需真实日志）

## 已直接改
- 品牌输入框默认填 `GOFU`（entry.brand 默认值 ''→'GOFU'）。

## 问题1：架类和花洒都没生出图（前端没渲染+上新也没有=真没生成）
- 两个品类**都**没出 → 不是架类专属分支问题。
- 现象"前端没图+上新没图"= 生图接口整批失败（context 里 mainImages/detailImages/SKU imgDir 都空）。
- 最可能：**这台机器网络连不上生图接口**（linapi.net），与之前日志里 `Read/Connection timed out` 同源，只是这次可能整批超时（上次是中途瞬断、退避扛过去了）。也可能密钥额度/配置问题。
- **必须看这次的 cloud.log**：搜 `花洒主图失败`/`生图失败`/`timed out`/`HTTP` 才能确诊是超时、限流、还是密钥。光代码定不了。
- 位置：解压目录 `app\data\logs\cloud.log`。

## 问题2：先选方案再生图（提速，参考乐羽）
- **乐羽做法**(app.js lyAutoRun:855)：全自动模式 `planCount = planMode==='1'?1:4`，但**只取 plans[0] 一套**去生图；SKU 图在用户「采用」后才对那一套生。即"多套只是布局候选，生图只生选定的一套"。
- **GOFU 现状**：step1 出全部方案布局；step2 对 selectedPlanIndex **一套**生 SKU 图（已经是一套，没全生）。但主图 6 张+详情 6 张是**不分方案**的商品级图。
- 用户说"三套方案以上生成太慢"——待确认现状到底慢在哪：若已经只生一套 SKU，那慢是因为 6+6+N 张同步排队。建议流程：**step1 只出布局(秒级)→ 用户在预览选定方案 → 再点"生成图片"只对选定方案生**（主图/详情/SKU）。即把生图从"一键到底"拆成"先布局选方案、再生图"。
- 待你确认：是否要把当前"一键生成(step1+step2连跑)"改成"两段式：先布局选方案 → 再生图"？(这是流程改动，符合你说的提速方向)

## 问题3：SKU名与产品款式不对应（三层锅盖架写成单层款）
- 根因：架类主件本身带层数属性(一层/二层/三层)，但 `generateSkuPlans` 的型号(specName)阶梯把"单层基础款/双层扩容款/三层旗舰款"当成了配件档位来编 → 与主件真实层数打架。
- 花洒的型号阶梯=配件组合(喷头/软管/滤芯)，架类不该套同一套"层数档位"话术。
- 修：prompt 里对架类明确"型号名只描述配件组合/数量档位，**禁止臆造层数/规格档位**，主件的层数等固有属性已在主件名里，型号不得再编"。属 prompt 工程。

## 待你提供/拍板
1. **问题1**：发这次的 `app\data\logs\cloud.log`（尤其生图报错那几行），我按实证定位。
2. **问题2**：确认是否改"两段式(先布局选方案→再生图)"流程。
3. **问题3**：prompt 加架类型号约束，直接做。
