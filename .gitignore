# Maven
target/
*.jar
!.mvn/wrapper/maven-wrapper.jar

# 构建产物
dist/
dist-electron/
out/

# Electron / Node
node_modules/
electron/secret.js

# 本地运行时二进制（体积大，不入 git，由 provision 脚本/playwright install 获取）
gofu-client-local/tools/node/
gofu-client-local/tools/browsers/
gofu-client-local/tools/*.exe

# Playwright 运行时产物（截图/cookies/诊断）
gofu-client-local/tools/*.png
gofu-client-local/tools/pdd_cookies.json
gofu-client-local/tools/sku_input_diag.json

# 数据库（含隐私，不提交）
*.db
*.db-shm
*.db-wal

# 密钥与本地配置（绝不提交）
application-local.yml
application-prod.yml
.env
*-session.json
user-prefs.json
# 快麦真实凭证（appSecret/accessToken 等，绝不提交）
kuaimai-config.json
kuaimai-sku-cache.json

# 便携打包产物与运行时（体积大/含登录态，不入库）
packaging/dist/
packaging/_runtime/
packaging/_input/
packaging/launcher/build/
*.zip
# 拼多多浏览器 profile（含登录态 Cookie，绝不提交）
gofu-client-local/tools/pdd_browser_profile/
# ERP 拉取的白底图 / 生成的 SKU 图（运行时产物）
erp-white/
# 多店铺运行时数据（stores.json 含店铺名映射，stores/ 含每店独立 cookie/profile，绝不提交）
stores.json
stores/
sku-gen/
# 标题库运行时缓存(userDataDir下,源是 gofu-server-cloud classpath prompt/title-lib.json)
title-lib.json
# 主图构图提示词素材(桌面拷入的xlsx源,不入库;已转成 main-compositions.json)。
# 注意:底层信息/ 其余文件(花洒配件信息/产品信息填写参考/架类防比价参考图等)已入库跟踪,不能整目录忽略。
底层信息/电商主图构图复刻提示词/
# 备份文件
*.bak-*

# 运行时临时目录
.temp-output/
.multipart-temp/
.history-refs/
.firecrawl/

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# 日志
*.log

# 验证证据（本地留存，不入库）
.verify-evidence/

# docs/ 整个目录为本地工作笔记,不入库
docs/

# 运行时产物(导入临时上传/生成资产)，不入库
listing-tmp/
sku-base/

# 批量流图形选文件夹上传重建的临时目录
batch-tmp/
